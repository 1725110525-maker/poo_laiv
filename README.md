# Repositorio de programacio orientada a objetos con python
repositorio de jercico de programacion 
## 1. crear .gitignore
crar el archivo .gitignore para configurar archivos y carpetas que no deseamos 
````shell
*.pyc
_pycache_/
````
3 2. Indexar archivos y carpetas 
Indexa todos los directorios y carpetas e busca de 
````shell
git  add .
````
## 3. crear un COMMIT
crear un comint o punto de control de los cambios realizados
````shell
git commit -m "CREATE .gitginore"

* CREATED -Se crearon nuevas carpeta so archivos.
* UPDARES -SE actuliza o agregaron nuevas funciones.
* FIXED -Se corrigeron errores.
## 4. Realizar un COMMIT
sincroniza los cambios realizados en el repositorio

````shell
git push -u origin main
````

#5.Agregar Documentacion alos metodos
agregar un **Docstring** alos metodos generados.
# metodo dos sirve para recibir valortes enteros, suma y devuelve el resultado de la suma realizada 