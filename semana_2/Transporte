class Transporte:
    def __init__(self, ruta, costo, color, num_asientos, num_llantas, num_puertas, max_peso, cilindros, marca, modelo):
        self.ruta = ruta
        self.costo = costo
        self.color = color
        self.num_asientos = num_asientos
        self.num_llantas = num_llantas
        self.num_puertas = num_puertas
        self.max_peso = max_peso
        self.cilindros = cilindros
        self.marca = marca
        self.modelo = modelo
        print(f"la ruta del transporte:{self.ruta}")
        print(f"el costo del transporte:{self.costo}")
        print(f"color del transporte:{self.color}")
        print(f"Numero de asientos:{self.num_asientos}")
        print(f"Numero de llantas:{self.num_llantas}")
        print(f"numero de puertas:{self.num_puertas}")
        print(f"Maximo peso que soporta el transporte:{self.max_peso}")
        print(f"cilidros del transporte:{self.cilindros}")
        print(f"Marca del transporte:{self.transporte:{self.marca}}")
        print(f"Modelo del transporte:{self.modelo}")
    def tomar_transporte(self):
        print("Tomar el transporte")
    def pagar(self):
        print("Pagar el transporte")
    def ver(self):
        print("ver pasar el transporte")
    def equivocarse(self):
        print("Equivocarse de transporte")
    def tiempo(self):
        print("Medir el tiempo del transporte")
Transporte_libramiento = Transporte ("libramiento", "12", "rojo", "10", "4", "4", "2", "300", "4", "toyota", " Hiace")
Transporte.tomar_transporte()
Transporte.pagar()
Transporte.ver()
Transporte.equivocarse()
Transporte.tiempo()